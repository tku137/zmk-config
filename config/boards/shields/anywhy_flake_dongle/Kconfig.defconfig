if SHIELD_ANYWHY_FLAKE_DONGLE
    config ZMK_KEYBOARD_NAME
        default "anywhy_flake_dongle"

    config ZMK_SPLIT
        default y
    config ZMK_SPLIT_ROLE_CENTRAL
        default y
    config ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS
        default 2

    config BT_MAX_CONN
        default 7
    config BT_MAX_PAIRED
        default 7

    config ZMK_STUDIO
        default n
endif
